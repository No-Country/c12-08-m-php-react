const AboutItemsContainer = ({ children }: { children: React.ReactNode }) => {
  return <section className='flex flex-col gap-8 py-8 pr-8'>{children}</section>;
};

export default AboutItemsContainer;
